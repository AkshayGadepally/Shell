// handle '' input
       
        StringBuilder result = Quotation.quote(input);

// if(Character.inWhiteSpace(c) && !isSingleQuoted && !isDoubleQuoted){
       if(CharacterToken.getLength() >0){
              Token.add(CharacterToken.getString(input))
       }
}
        
